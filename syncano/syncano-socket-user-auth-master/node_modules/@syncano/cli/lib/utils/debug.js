'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _raven = require('raven');

var _raven2 = _interopRequireDefault(_raven);

var _debug = require('debug');

var _debug2 = _interopRequireDefault(_debug);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// To use this util you need to initiate logger, e.g.:
// import logger from '../utils/debug';
// const { info, debug } = logger('<my_logger_name>');

const logTypes = ['debug', 'info', 'warn', 'error'];

const logger = name => {
  const functions = {};
  logTypes.forEach(type => {
    functions[type] = (...args) => {
      args.forEach((arg, index) => {
        if (index === 0) {
          _raven2.default.captureBreadcrumb({
            message: arg,
            category: name,
            level: type
          });
          (0, _debug2.default)(`${name}:${type}`)(arg);
        } else {
          (0, _debug2.default)(`${name}:${type}:verbose`)(arg);
        }
      });
    };
  });
  return functions;
};

exports.default = logger;
module.exports = exports['default'];
//# sourceMappingURL=debug.js.map