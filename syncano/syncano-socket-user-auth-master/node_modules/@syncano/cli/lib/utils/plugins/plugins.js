'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _debug = require('../debug');

var _debug2 = _interopRequireDefault(_debug);

var _printTools = require('../print-tools');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const { debug } = (0, _debug2.default)('utils-plugins');

class Plugins {
  constructor(session) {
    this.session = session;
    this.plugins = session.settings.project.getPlugins() || [];
  }

  load(program) {
    debug('load()');
    Object.keys(this.plugins).forEach(pluginName => {
      /* eslint-disable import/no-dynamic-require */
      /* eslint-disable global-require */
      debug('loading plugin:', pluginName);
      try {
        const PluginImport = require(this.plugins[pluginName]).default;
        program.command(pluginName).description(pluginName).action((...options) => {
          new PluginImport().run({ session: this.session, options });
        });
      } catch (err) {
        (0, _printTools.warning)('Error while loading plugin:', this.plugins[pluginName]);
      }
    });
  }
}
exports.default = Plugins;
module.exports = exports['default'];
//# sourceMappingURL=plugins.js.map